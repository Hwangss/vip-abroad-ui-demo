/* ==========================================================================
   设置宽高
   ========================================================================== */

// 宽高
size($width, $height)
    width $width
    height $height

// 正方
square($l)
    size($l, $l)
